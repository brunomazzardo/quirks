{
  "name": "quirks-server",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "quirks": "./src/bin.ts"
  },
  "type": "module",
  "scripts": {
    "dev": "node --watch src/dev.ts",
    "typecheck": "tsgo --noEmit",
    "test": "vp test run --passWithNoTests"
  },
  "dependencies": {
    "@effect/platform-node": "catalog:",
    "@effect/platform-node-shared": "catalog:",
    "@quirks/shared": "workspace:*",
    "commander": "^15.0.0",
    "effect": "catalog:",
    "node-pty": "^1.1.0"
  },
  "devDependencies": {
    "@effect/vitest": "catalog:",
    "@quirks/contracts": "workspace:*",
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "vite-plus": "catalog:"
  }
}
