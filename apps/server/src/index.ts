// QK-MONO-003 lands the store and HTTP service here; QK-MONO-004 adds the CLI
// and the `quirks` bin. Until then the bun-era src/ at the repo root serves both.
export const packageName = "quirks-server";
